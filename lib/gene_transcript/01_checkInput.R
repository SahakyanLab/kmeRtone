checkInput <- function(genome.name, genome.annotation, k, ref.kmers,
                       output.file, genome.path, genome.prefix, genome.suffix,
                       genome.annotation.path){




}
